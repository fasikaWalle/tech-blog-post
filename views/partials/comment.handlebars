<div class="blog-post">
    {{#each this}}
    <div class="card">
        <div class="card-body">
            <p>{{comment_text}}</p>
            <span>- {{user.username}} , {{ format_date created_at}}</span>
        </div>
    </div>
</div>
{{/each}}